language: rust

rust:
  - nightly
  - stable

before_script:
  - rustup component add rustfmt

script:
  - cargo fmt --all -- --check
  - cargo build

notifications:
  webhooks: http://build.servo.org:54856/travis
